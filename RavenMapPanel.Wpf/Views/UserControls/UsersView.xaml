<UserControl x:Class="RavenMapPanel.Views.UserControls.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="900"
             Background="#0B0D0C">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#151816" BorderBrush="#2D322E" BorderThickness="1" CornerRadius="2" Padding="18" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="KULLANICI YÖNETİMİ" Style="{StaticResource HeaderTextStyle}" Foreground="#E7E8E5"/>
                <TextBlock Text="Yönetici ve kullanıcı hesaplarını yönetin" Style="{StaticResource SubheaderTextStyle}" Foreground="#8F978F"/>
            </StackPanel>
        </Border>
        
        <!-- Users List -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            
            <DataGrid x:Name="UsersDataGrid" 
                      Grid.Column="0"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Kullanıcı Adı" Binding="{Binding Username}" Width="150"/>
                    <DataGridTextColumn Header="E-posta" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Rol" Binding="{Binding Role}" Width="100"/>
                    <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Son Giriş" Binding="{Binding LastLogin, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
            
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Border Background="#151816" BorderBrush="#2D322E" BorderThickness="1" CornerRadius="2" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Yeni Kullanıcı" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,10"/>
                        
                        <Label Content="Kullanıcı Adı" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="UsernameTextBox" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,10"/>
                        
                        <Label Content="E-posta" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="EmailTextBox" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,10"/>
                        
                        <Label Content="Şifre" Style="{StaticResource LabelStyle}"/>
                        <PasswordBox x:Name="PasswordBox" Style="{StaticResource PasswordBoxStyle}" Margin="0,0,0,10"/>
                        
                        <Label Content="Rol" Style="{StaticResource LabelStyle}"/>
                        <ComboBox x:Name="RoleComboBox" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,10">
                            <ComboBoxItem Content="Yönetici"/>
                            <ComboBoxItem Content="Kullanıcı"/>
                            <ComboBoxItem Content="Gözlemci"/>
                        </ComboBox>
                        
                        <Button x:Name="CreateUserButton" Content="Kullanıcı Oluştur" 
                                Style="{StaticResource PrimaryButtonStyle}" Width="150" Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>
                
                <Border Background="#151816" BorderBrush="#2D322E" BorderThickness="1" CornerRadius="2" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Kullanıcı İşlemleri" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,10"/>
                        
                        <Button x:Name="EditUserButton" Content="Düzenle" 
                                Style="{StaticResource SecondaryButtonStyle}" Width="150" Margin="0,0,0,10"/>
                        
                        <Button x:Name="DeleteUserButton" Content="Sil" 
                                Style="{StaticResource DangerButtonStyle}" Width="150"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#111411" BorderBrush="#2D322E" BorderThickness="1" CornerRadius="2" Padding="15" Margin="0,15,0,0">
            <TextBlock x:Name="StatusTextBlock" Text="Hazır" Style="{StaticResource StatusTextStyle}"/>
        </Border>
    </Grid>
</UserControl>
