<UserControl x:Class="RavenMapPanel.Views.ModernAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1100"
             Background="#0B0D0C">
    <UserControl.Resources>
        <Style x:Key="AdminNavButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#9DA49D"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="13,11"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="MinHeight" Value="43"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="#191C1A"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="NavBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <Border Grid.Column="0" Background="#0C0F0D" BorderBrush="#252A26" BorderThickness="0,0,1,0">
            <Grid Margin="12,18,12,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="8,0,8,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="42"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/RavenMapPanel;component/Assets/raven-brand-v3.png" Width="38" Height="38" Stretch="UniformToFill"/>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="RAVEN MAP" Foreground="#E7E8E5" FontSize="17" FontWeight="Bold"/>
                        <TextBlock Text="CONTROL CENTER" Foreground="#8F978F" FontSize="9"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Height="1" Background="#252A26" Margin="0,0,0,14"/>

                <StackPanel Grid.Row="2">
                <Button Name="ServerConfigButton" Click="ServerConfigButton_Click" 
                        Style="{StaticResource AdminNavButtonStyle}" Tag="ServerConfig">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="SY" FontFamily="Consolas" FontSize="9" Foreground="#6F786F" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Sunucu Ayarları" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Grid>
                </Button>

                <Button Name="UpdatesButton" Click="UpdatesButton_Click" 
                        Style="{StaticResource AdminNavButtonStyle}" Tag="Updates">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="GN" FontFamily="Consolas" FontSize="9" Foreground="#6F786F" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Güncellemeler" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Grid>
                </Button>

                <Button Name="UsersButton" Click="UsersButton_Click" 
                        Style="{StaticResource AdminNavButtonStyle}" Tag="Users">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="KA" FontFamily="Consolas" FontSize="9" Foreground="#6F786F" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Kullanıcılar" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Grid>
                </Button>

                <Button Name="LogsButton" Click="LogsButton_Click" 
                        Style="{StaticResource AdminNavButtonStyle}" Tag="Logs">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="LS" FontFamily="Consolas" FontSize="9" Foreground="#6F786F" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Günlükler" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Grid>
                </Button>
                </StackPanel>

                <Border Grid.Row="3" BorderBrush="#252A26" BorderThickness="0,1,0,0" Padding="8,12,8,0">
                    <StackPanel>
                        <TextBlock Text="RAVEN MAP ADMIN" Foreground="#8F978F" FontSize="9" FontWeight="Bold"/>
                        <TextBlock Text="Yerel yönetim arayüzü" Foreground="#5F675F" FontSize="10" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1" Background="#0B0D0C">
            <Frame Name="ContentFrame" NavigationUIVisibility="Hidden" Background="#0B0D0C"/>
        </Grid>
    </Grid>
</UserControl>
